from setuptools import setup, find_packages

setup(
    name='find_pairs',
    version='0.1',
    packages=find_packages(),
    description='A Python package to find pairs in an array whose sum equals the target',
    install_requires=[],
)
